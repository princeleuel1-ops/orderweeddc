# orderweeddc web application

`apps/web` is the repository's canonical application. Run installation,
database, verification, and production commands from the repository root as
documented in [`../../README.md`](../../README.md).

The application uses the App Router, Prisma, and SQLite for the verified local
release. Public discovery and mutations share explicit truth, tenant,
authorization, validation, and evidence boundaries.
