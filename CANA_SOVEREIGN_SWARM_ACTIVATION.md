# CANA SOVEREIGN SWARM — BUILD, LAUNCH, VERIFY, AND CONTINUE

You are the bootstrap engineer responsible for creating and launching CANA’s independent, durable, five-lane intelligence factory.

This is an implementation and activation order.

Do not return another architecture proposal.
Do not merely summarize the current repository.
Do not stop after creating placeholder files.
Do not stop after one cycle.
Do not use Codex or Antigravity as the long-running loop.

## PROJECT

Repository:

The current Git worktree containing this directive.

Current baseline:

- The monorepo builds locally.
- Existing tests, lint, TypeScript, production build, authentication, truthful-data protections, provenance controls, claim/correction protections, accessibility work, SEO controls, and CANA Governor v3 must be preserved.
- The deterministic local quality loop remains useful as a verification gate.
- The missing deliverable is the independent model-driven swarm that keeps operating after Codex closes.

## ULTIMATE MISSION

Transform CANA / Order Weed DC into the most ambitious, truthful, technically excellent, commercially valuable, secure, accessible, evidence-backed cannabis discovery and intelligence platform that this repository can support.

The system must continuously:

- discover weaknesses;
- research better approaches;
- generate high-value missions;
- implement improvements;
- attack its own implementation;
- test every material claim;
- reject weak work;
- repair rejected work;
- integrate only proven improvements;
- measure whether the product actually became stronger;
- immediately begin the next mission.

Ambition does not mean uncontrolled rewriting.

Ambition means:

- category-defining product thinking;
- original design and functionality;
- exceptional engineering;
- excellent mobile and desktop experiences;
- trustworthy data;
- measurable performance;
- strong accessibility;
- useful merchant and administrator tools;
- superior discovery, search, filters, neighborhoods, listings, menus, deals, education, provenance, disputes, and correction workflows;
- rigorous security and authorization;
- excellent SEO and answer-engine structure;
- durable automation;
- no fake claims or fake evidence;
- no destruction of verified progress.

## PRIMARY OUTCOME

Build and launch a local Windows orchestration system that:

1. Runs independently after Codex and Antigravity close.
2. Uses OpenCode as the headless coding-agent runtime.
3. Uses OpenRouter directly for model inference.
4. Uses five separately configured API-secret references.
5. Operates five canonical intelligence lanes.
6. Runs through short, durable, recoverable missions rather than one fragile endless conversation.
7. Can continue for at least 96 hours.
8. Can continue beyond 96 hours through an explicit continuous mode or durable extension.
9. Survives supervisor crashes, OpenCode crashes, lane crashes, rate limits, provider failures, and Windows restarts.
10. Self-verifies every change repeatedly.
11. Never permits a model to approve its own work.
12. Continues useful local work when one or more model lanes are temporarily unavailable.
13. Maintains truthful evidence of all work.
14. Stops only for a genuine human-only or safety-critical boundary, verified release completion, or an explicit stop command.

## BOOTSTRAP BOUNDARY

Codex is only the engineer constructing and launching the machine.

The completed runtime must not invoke:

- `codex`;
- `codex exec`;
- ChatGPT;
- Antigravity;
- an interactive coding subscription;
- any recursive Codex session.

After successful launch, closing Codex must not terminate:

- the CANA supervisor;
- OpenCode’s headless service;
- active missions;
- lane state;
- the mission queue;
- the watchdog;
- the heartbeat;
- the control tower.

## CREATE THE RUNTIME

Create a first-class subsystem:

`CANA_LOOP_ENGINE\`

Use a clear, maintainable implementation. Python plus PowerShell is preferred unless repository evidence proves another stack is materially stronger.

At minimum create and fully connect:

- `supervisor.py`
- `state_store.py`
- `mission_queue.py`
- `objective_engine.py`
- `opencode_adapter.py`
- `openrouter_health.py`
- `lane_manager.py`
- `worktree_manager.py`
- `research_ledger.py`
- `critic_gate.py`
- `verification_gate.py`
- `release_judge.py`
- `integration_gate.py`
- `rollback_manager.py`
- `heartbeat.py`
- `watchdog.py`
- `no_progress_detector.py`
- `reporter.py`
- `config\lanes.example.json`
- `config\runtime.example.json`
- `prompts\strategy.md`
- `prompts\truth.md`
- `prompts\implementation.md`
- `prompts\verification.md`
- `prompts\release-judge.md`
- `Start-CanaLoop.ps1`
- `Stop-CanaLoop.ps1`
- `Get-CanaLoopStatus.ps1`
- `Extend-CanaLoop.ps1`
- `Resume-CanaLoop.ps1`
- `Install-CanaLoopWatchdog.ps1`
- `Uninstall-CanaLoopWatchdog.ps1`
- `Test-CanaLoopRecovery.ps1`
- `README.md`
- `ARCHITECTURE.md`
- `OPERATIONS_WINDOWS.md`
- `SECURITY_MODEL.md`
- `RECOVERY.md`
- `MISSION_PROTOCOL.md`
- `RELEASE_STANDARD.md`

Do not create disconnected placeholders. Every component must participate in the actual runtime or be removed.

## OPENCODE RUNTIME

Use OpenCode headlessly.

Integrate the supported equivalent of:

- `opencode serve`;
- `opencode run`;
- attaching jobs to a persistent server;
- machine-readable event output;
- explicit model selection;
- project-directory scoping;
- session continuation;
- controlled permissions.

Do not require the OpenCode desktop interface or TUI to remain open.

Disable public sharing.

Bind the headless server to localhost unless a stronger secure local mechanism is demonstrated.

Protect the server with locally managed authentication when supported.

Restrict model tools and shell execution to the CANA repository, approved worktrees, and approved temporary directories.

## FIVE-API SWARM

Treat the five configured API secrets as five independently managed worker lanes.

Never put plaintext credentials in:

- Git;
- Markdown;
- JSON configuration;
- command arguments;
- PowerShell history;
- process titles;
- logs;
- screenshots;
- test fixtures;
- control-tower reports.

Use the existing approved CANA secret-loading mechanism or Windows Credential Manager.

Configuration must contain secret reference names only.

If all five accepted secret references are available, use them.

Do not weaken, delete, bypass, or silently override the repository’s credential preflight protections.

Do not repeatedly lecture the operator about credentials. Record a credential-specific blocker once, continue all independent construction and testing, and automatically proceed when an accepted secret reference becomes available.

## THE FIVE CANONICAL LANES

### LANE 1 — SOVEREIGN STRATEGY

This lane acts as chief product architect and mission planner.

It must:

- inspect the verified repository state;
- understand the ultimate product mission;
- identify the most consequential weaknesses;
- create bounded missions;
- define measurable acceptance criteria;
- rank missions by user value, commercial value, trust improvement, risk reduction, evidence quality, implementation cost, and reversibility;
- prevent duplicated, stale, cosmetic-only, or low-value missions;
- maintain an ambitious multi-horizon roadmap;
- protect working systems from needless rewrites.

It may propose work but may not provide final approval for implemented work.

### LANE 2 — TRUTH, RESEARCH, COMPLIANCE, SEO, AND AEO

This lane is the evidence and truth authority.

It must:

- challenge unsupported assumptions;
- inspect provenance and freshness;
- research authoritative sources when external research is available;
- record source, retrieval time, claim supported, confidence, and limitations;
- detect synthetic or stale data;
- examine cannabis-information boundaries;
- improve technical SEO, structured data, internal linking, canonicalization, crawl controls, and answer-ready factual architecture;
- reject fake reviews, fake rankings, fake licenses, fake inventory, fake deals, fake hours, fake delivery promises, fake compliance claims, and fake popularity;
- distinguish verified, submitted, third-party, demonstration, disputed, expired, and unknown information.

It may not certify facts without evidence.

### LANE 3 — IMPLEMENTATION

This lane is the principal engineering and product-building worker.

It must:

- implement frontend, backend, database, API, search, product, merchant, admin, analytics, governor, testing, and infrastructure improvements;
- use isolated Git worktrees;
- make bounded reviewable changes;
- create or update tests with changed behavior;
- preserve existing truth, security, authentication, authorization, provenance, and validation guarantees;
- use original product and design work;
- avoid copying protected branding, text, reviews, datasets, or trademark-confusing presentation;
- document changed files and expected behavior;
- never merge or approve its own work.

### LANE 4 — ADVERSARIAL VERIFICATION

This lane assumes the implementation is wrong until evidence proves otherwise.

It must inspect:

- correctness;
- regressions;
- hidden failure modes;
- security;
- authorization;
- privacy;
- data truth;
- legal/compliance risk;
- accessibility;
- responsive behavior;
- performance;
- SEO/AEO;
- broken journeys;
- misleading UX;
- placeholder behavior;
- test quality;
- mutation safety;
- transaction safety;
- concurrency;
- rollback readiness.

It must produce:

- explicit findings;
- severity;
- reproduction steps;
- affected files;
- required repair;
- acceptance or rejection.

It cannot approve work it authored.

### LANE 5 — RELEASE JUDGE AND INTEGRATOR

This lane is the final independent gate.

It must:

- inspect the original mission and acceptance criteria;
- inspect the complete diff;
- inspect Lane 4’s adversarial review;
- inspect deterministic test evidence;
- verify rollback information;
- detect file conflicts;
- ensure no unresolved critical or high-severity defect remains;
- reject unproven claims;
- authorize local integration only when evidence is sufficient;
- trigger post-integration regression verification;
- revert or reopen work when post-integration verification fails.

It must not implement the original change it is judging.

## DURABLE MISSION PROTOCOL

Do not attempt one uninterrupted 96-hour model response.

Use short, checkpointed missions.

Persist mission state in SQLite.

Every mission must include:

- mission ID;
- parent mission;
- objective;
- rationale;
- lane;
- primary model;
- fallback models;
- secret reference;
- priority;
- dependencies;
- measurable acceptance criteria;
- prohibited changes;
- worktree;
- lease owner;
- lease start;
- lease expiration;
- attempt number;
- maximum attempts;
- OpenCode session ID;
- prompt hash;
- input-state hash;
- changed files;
- diff hash;
- commands executed;
- test evidence;
- critic findings;
- repair history;
- release-judge decision;
- integration commit or patch reference;
- rollback reference;
- blocker classification;
- next action;
- timestamps.

Support at least these states:

- `queued`
- `leased`
- `planning`
- `researching`
- `implementing`
- `awaiting_criticism`
- `rejected`
- `repairing`
- `awaiting_verification`
- `awaiting_release_judgment`
- `accepted`
- `integrating`
- `integrated`
- `post_integration_verification`
- `retry_wait`
- `blocked_external`
- `blocked_human`
- `failed_retryable`
- `failed_terminal`
- `superseded`
- `completed`

Expired leases must be recovered.

An orphaned mission must never remain permanently stuck.

All mutating operations must be idempotent or protected by durable operation identifiers so retrying does not duplicate side effects.

## THE NONSTOP SOVEREIGN LOOP

The supervisor must continuously execute:

1. Read the ultimate mission.
2. Read current repository and control-tower state.
3. Recompute the highest-value unblocked weaknesses.
4. Generate bounded candidate missions.
5. Challenge each candidate for usefulness and duplication.
6. Rank candidates.
7. Lease the strongest mission.
8. Assign the proper lane and model.
9. Create an isolated worktree for modifying work.
10. Execute research or implementation.
11. Save all artifacts and evidence.
12. Send the work to an independent adversarial lane.
13. Reject and repair weak work.
14. Run deterministic targeted tests.
15. Run appropriate regression gates.
16. Send accepted work to the independent release judge.
17. Integrate only when approved.
18. Run post-integration verification.
19. Automatically revert or reopen the mission if post-integration verification fails.
20. Update the product scorecard and evidence ledger.
21. Immediately select the next mission.
22. Repeat.

Do not stop because one task finished.

Do not stop because one lane crashed.

Do not stop because a model returned malformed output.

Do not stop because a provider temporarily rate-limited requests.

Do not stop because a test failed.

A failed test is work to repair, not a reason to terminate the entire loop.

## SELF-VERIFICATION STANDARD

Every material change must be checked at least four times:

1. The author performs targeted checks.
2. Lane 4 performs adversarial review.
3. Deterministic verification runs.
4. Lane 5 performs independent release judgment.

After integration, run a fifth check:

5. Post-integration regression verification against the real integrated branch.

When any check fails:

- preserve evidence;
- reject the change;
- create a bounded repair mission;
- assign the repair;
- repeat independent review;
- do not lower the standard merely to pass.

No model may mark its own work complete.

No passing statement is valid without command output, artifact evidence, or a reproducible inspection result.

## QUALITY CONSTITUTION

The loop must continually push toward these release conditions:

- zero unresolved critical security defects;
- zero unresolved high-severity authorization defects;
- zero production-facing demonstration data presented as verified;
- zero unsupported public factual claims;
- zero failing required quality gates;
- zero lint errors and warnings;
- passing TypeScript;
- passing production build;
- passing unit, integration, security, truth, HTTP, browser, database, authentication, authorization, governor, and recovery tests;
- protected merchant and administrator mutations;
- transactional integrity;
- duplicate prevention;
- validated price, duration, deal, inventory, menu, and profile mutations;
- strong mobile and desktop journeys;
- no critical dead ends or inert primary controls;
- accessible structure and keyboard operation;
- measured performance budgets;
- truthful metadata and structured data;
- clean canonical and indexing behavior;
- comprehensive provenance and freshness;
- reversible integrations;
- durable operations documentation;
- complete deployment and rollback plan;
- qualified human review clearly identified where required.

The system must not invent evidence or manipulate the score merely to declare victory.

## AMBITION ENGINE

Do not allow the loop to become a small bug-fixing robot.

Maintain three horizons:

### HORIZON A — RELEASE INTEGRITY

Finish all work required to make the existing product truthful, secure, reliable, testable, deployable, and reviewable.

### HORIZON B — CATEGORY LEADERSHIP

Build original advantages such as:

- verification freshness;
- transparent source history;
- neighborhood intelligence;
- high-quality discovery;
- strong filters;
- evidence-aware deals and menus;
- merchant self-service;
- correction and dispute transparency;
- consumer trust tools;
- accessible education;
- excellent performance;
- superior mobile UX;
- explainable ranking logic;
- attribution and merchant insights;
- useful administrator operations.

### HORIZON C — CATEGORY CREATION

Propose and test original capabilities that are not merely copies of established directories.

Every ambitious proposal must include:

- user problem;
- evidence;
- differentiation;
- implementation path;
- trust and legal risks;
- success metric;
- experiment design;
- rollback;
- cost;
- dependency;
- reason it deserves priority.

Do not confuse visual decoration with innovation.

## NO-PROGRESS PROTECTION

“Nonstop” must mean nonstop useful improvement, not wasteful repetition.

Detect:

- identical prompts;
- repeated identical failures;
- no changed files;
- no new evidence;
- no acceptance-criteria movement;
- no score improvement;
- repeated reversion;
- circular debates;
- cosmetic churn;
- uncontrolled refactoring.

When the threshold is reached:

1. Stop the repeated approach.
2. Diagnose why it failed.
3. Narrow or decompose the mission.
4. Change model or lane.
5. Request stronger evidence.
6. Create a reconciliation mission.
7. Classify a real blocker if necessary.
8. Continue another valuable workstream.

## PROVIDER RESILIENCE

Implement:

- `429` handling;
- `503` and provider-overload handling;
- `Retry-After`;
- exponential backoff;
- jitter;
- per-key cooldown;
- per-model cooldown;
- per-lane cooldown;
- circuit breakers;
- timeout classification;
- malformed-response recovery;
- fallback models;
- daily request accounting;
- context-budget limits;
- provider-health status;
- maximum retry ceilings.

Do not assume five keys multiply platform-level limits.

Distribute work across models intelligently.

When inference is unavailable, continue deterministic local work:

- tests;
- lint;
- TypeScript;
- builds;
- schema validation;
- database audits;
- unsupported-claim scans;
- secret scans;
- link checks;
- duplicate detection;
- provenance checks;
- accessibility checks;
- performance checks;
- evidence indexing;
- report generation;
- cleanup of approved temporary resources.

## GIT AND FILE SAFETY

Never allow multiple modifying missions to edit the same primary working tree.

Use isolated Git worktrees.

Before integration:

- inspect overlap;
- preserve both diffs;
- reject silent overwrites;
- create a reconciliation mission for conflicts;
- run full applicable tests after reconciliation.

Never:

- force-push;
- rewrite shared history;
- delete the primary repository;
- erase evidence;
- remove tests to pass;
- replace tests with no-op commands;
- broadly suppress lint;
- disable security controls;
- automatically deploy publicly;
- purchase anything;
- contact third parties;
- create public claims without evidence.

Local integrations must remain reviewable and reversible.

## WINDOWS DURABILITY

Create these working commands:

### Install

`\.\CANA_LOOP_ENGINE\Install-CanaLoopWatchdog.ps1`

### Preflight

`\.\CANA_LOOP_ENGINE\Start-CanaLoop.ps1 -PreflightOnly`

### Start four-day run

`\.\CANA_LOOP_ENGINE\Start-CanaLoop.ps1 -Hours 96 -MaxParallelLanes 5`

### Start continuous goal-driven mode

`\.\CANA_LOOP_ENGINE\Start-CanaLoop.ps1 -UntilReleaseReady -MaxParallelLanes 5`

Continuous mode must still use bounded missions, resource ceilings, weekly checkpoints, and safe stop controls.

### Status

`\.\CANA_LOOP_ENGINE\Get-CanaLoopStatus.ps1`

### Resume

`\.\CANA_LOOP_ENGINE\Resume-CanaLoop.ps1`

### Extend

`\.\CANA_LOOP_ENGINE\Extend-CanaLoop.ps1 -Hours 96`

### Stop safely

`\.\CANA_LOOP_ENGINE\Stop-CanaLoop.ps1`

The watchdog must:

- start or resume the authorized runtime at user logon or startup;
- restart the supervisor after failure;
- prevent duplicate supervisors;
- respect a manual-stop marker;
- respect the configured ending condition;
- avoid storing secrets;
- write launch failures;
- distinguish a long-running build from a dead process;
- never restart a runtime marked completed.

Write a heartbeat every 30 seconds or less.

## CONTROL TOWER

Continue existing records and add:

- `CANA_CONTROL_TOWER/RUNTIME_HEALTH.md`
- `CANA_CONTROL_TOWER/LANE_STATUS.md`
- `CANA_CONTROL_TOWER/MISSION_QUEUE.md`
- `CANA_CONTROL_TOWER/CYCLE_HISTORY.md`
- `CANA_CONTROL_TOWER/INTEGRATION_HISTORY.md`
- `CANA_CONTROL_TOWER/REJECTION_HISTORY.md`
- `CANA_CONTROL_TOWER/AMBITION_ROADMAP.md`
- `CANA_CONTROL_TOWER/RESEARCH_LEDGER.md`
- `CANA_CONTROL_TOWER/RELEASE_READINESS.md`
- `CANA_CONTROL_TOWER/EXTERNAL_LOOP_STATUS.json`

Do not falsely label the deterministic local loop as external model execution.

Routine updates belong in files and structured logs.

Do not flood the operator with repetitive narration.

## TRUE HUMAN-ONLY BLOCKERS

Interrupt the operator only for:

- an unavailable required secret or account authorization;
- money or purchasing;
- irreversible deletion;
- public deployment;
- force-pushing shared history;
- a legal determination requiring qualified counsel;
- a material business decision that changes the product’s identity or operating model;
- a physical-world task.

Before stopping for a blocker, prove that no other meaningful independent mission remains.

Record each repeated blocker once.

Do not repeatedly return the same warning.

## ACCEPTANCE TESTS

The engine is not complete until demonstrated:

1. Codex closes and the supervisor remains active.
2. The runtime invokes OpenCode and never invokes Codex.
3. OpenCode runs headlessly.
4. OpenRouter is the configured inference provider.
5. Five secret references are independently addressable.
6. Five lanes are independently schedulable.
7. Mission state survives supervisor termination.
8. Mission state survives OpenCode termination.
9. Mission state survives a Windows restart simulation or actual restart-safe test.
10. Stale leases recover.
11. A Lane 5 crash recovers.
12. A simulated `429` honors cooldown.
13. A simulated `503` honors retry behavior.
14. A primary-model failure activates a fallback.
15. Modifying lanes use isolated worktrees.
16. File conflicts create reconciliation work instead of silent overwrite.
17. Failed tests block integration.
18. Critic rejection causes repair.
19. Release-judge rejection blocks integration.
20. Post-integration failure triggers rollback or reopening.
21. Accepted changes have rollback references.
22. No plaintext secret appears in Git, logs, reports, commands, or process metadata.
23. Existing application tests remain green.
24. Existing governor tests remain green.
25. ESLint remains at zero errors and warnings.
26. TypeScript passes.
27. Production build passes.
28. Recovery tests pass.
29. Status accurately reports active lanes, models, missions, cooldowns, heartbeat, last progress, and next actions.
30. The four-day mode stops cleanly.
31. Continuous mode continues through bounded missions until its verified objective condition or an explicit stop.
32. At least one real, bounded five-lane mission completes using accepted external provider access when available.

## EXECUTION ORDER

1. Read the existing repository and control-tower evidence.
2. Preserve all verified application improvements.
3. Inspect CANA Governor v3 and the deterministic quality loop.
4. Verify OpenCode availability.
5. Build the SQLite state store.
6. Build mission generation, leasing, and recovery.
7. Build the OpenCode headless adapter.
8. Build configurable five-lane routing.
9. Build model-health, retry, cooldown, and fallback logic.
10. Build isolated worktree management.
11. Build critic, deterministic verification, release judgment, integration, and rollback.
12. Build heartbeat and no-progress detection.
13. Build PowerShell lifecycle commands.
14. Build and install the Windows watchdog.
15. Build recovery and failure simulations.
16. Run every existing project quality gate.
17. Repair all failures.
18. Run a mocked five-lane end-to-end mission.
19. Run one real bounded five-lane mission when accepted provider access is available.
20. Launch the requested 96-hour run automatically when preflight passes.
21. Prove that the runtime remains alive after the Codex bootstrap process exits.
22. Write the exact handoff and status commands.

Do not stop after completing step 18 when step 19 is unblocked.

Do not return another proposal before implementing.

Do not claim the swarm is running unless process, heartbeat, mission, model, and OpenCode evidence prove it.

## FINAL RESPONSE

Return only:

### BUILT

Exact components and files implemented.

### LAUNCHED

Supervisor PID, OpenCode process or server evidence, configured ending condition, active lanes, and first mission IDs.

### VERIFIED

Exact commands and exact results for:

- mocked five-lane mission;
- real provider preflight;
- real bounded mission when available;
- supervisor crash;
- OpenCode crash;
- stale lease;
- Lane 5 crash;
- `429`;
- `503`;
- fallback;
- worktree isolation;
- conflict handling;
- critic rejection;
- release-judge rejection;
- integration rollback;
- watchdog restart;
- application quality gates.

### PROOF CODEX IS OUTSIDE THE LOOP

Evidence that the runtime process tree and source contain no Codex invocation.

### START

The exact PowerShell start command.

### STATUS

The exact status command.

### STOP

The exact safe-stop command.

### RESUME

The exact recovery command.

### REMAINING HUMAN-ONLY BLOCKERS

Genuine blockers only, each stated once.

Begin implementation and activation now.
