{
  "name": "orderweeddc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/ad-creative",
    "packages/ai"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint -w apps/web",
    "check": "npm run lint -w apps/web"
  },
  "overrides": {
    "postcss": "8.5.22",
    "sharp": "0.35.3"
  }
}
